---
type: project_requirements
schema_version: 1
project: Personal Ops PWA
version_scope: v2 (current)
updated: 2026-07-19
status: active-dev
handoff: true
---

# Personal Ops PWA — 项目需求文档（接手指南）

> **本文档目的**：完整记录 Personal Ops PWA 的产品需求、设计决策、当前实现状态与技术架构，使任何 AI 编程工具都能直接接手继续开发，无需重新理解上下文。
>
> **当前版本**：v2（2026-07-19 完成重构 + UI 修复，可运行）
> **源码位置**：`~/obsidian_vault/02_Operations/Workspaces/personal-ops/`（本目录）
> **开发源位置**：`~/.qclaw/workspace/personal-ops-pwa/`（含 node_modules，可 `npm install` 后 `npm run dev`）

---

## 0. 接手速览（30 秒读懂）

- **这是什么**：一个 PWA（渐进式 Web 应用），作为"个人 AI 工作台 + GitHub 知识库入口 + 任务看板"的统一控制台。
- **谁用**：手机 OS 产品经理（用户 JK），三端（手机 / 公司电脑 / 个人电脑）共用同一网址。
- **技术栈**：Vite 5 + React 18 + TypeScript + React Router 6 + Zustand 4 + vite-plugin-pwa（service worker 离线缓存）。
- **当前实现了什么**：登录页（模拟 GitHub 登录）+ 3 个核心页面（知识库浏览 / AI 助理数据看板 / 设置），纯黑极简 Apple HMI 风格，桌面侧边栏 + 手机底部导航响应式。
- **当前是假的（mock）**：登录未接真实 GitHub OAuth；知识库内容、AI 数据均为本地模拟数据。
- **下一步重点**：接真实 GitHub（OAuth 登录 + 读取仓库文件树/内容）、补齐任务看板与 AI 审批中心（类型已定义）。

---

## 1. 产品背景与目标

用户是**手机 OS 产品经理**，日常使用三个终端：手机、公司电脑、个人电脑。

**核心痛点**：如果为三端分别开发 Android App / Mac App / Windows App，成本极高。

**PWA 解决方案**：只开发一套网页，即可覆盖三端——手机浏览器"添加到主屏幕"后像独立 App（桌面图标、全屏、免商店下载、自动更新、可保持登录、可离线缓存）。

**关键澄清**（防止接手 AI 走偏）：
- PWA 解决的是**多端界面统一**，并不直接解决 GitHub 数据同步和 AI 执行。
- GitHub 同步、权限、AI 任务仍需要**后台服务**处理（本期 v2 未做后台，数据全在前端 mock）。

**产品定位**：
```
你
↓ Personal Ops（产品层，本 PWA）
↓ 后台服务器 / GitHub App
↓ GitHub 私有仓库（知识库、任务、AI 产物）
```
Personal Ops 位于"你和 GitHub 之间"，承担四件事：**查看 / 编辑 / 调用 AI / 审批 AI**。它不是知识库本身，也不是 GitHub。

---

## 2. 产品功能定义

### 2.1 查看
查看 GitHub 中的：知识文档、项目、任务、决策、AI 执行结果。

### 2.2 编辑
在手机或电脑上：新增文档、修改内容、删除内容、创建任务，最终提交到 GitHub（本期未实现，仅类型预留）。

### 2.3 调用 AI
例如：总结文档、拆解任务、调研竞品、修改 PRD、准备周报（本期 AI 助理页为"数据看板"形态，非对话式调用）。

### 2.4 审批 AI
AI 先生成修改建议 → 用户查看差异 → 批准 / 修改后批准 / 驳回 → 批准后提交 GitHub。**AI 不能直接提交 GitHub**（权限表核心约束）。

---

## 3. 多终端统一登录设计（原始规划，v2 部分落地）

### 3.1 登录目标
```
一个网址 · 一个账号 · 一套权限 · 三端都能用
入口推荐：https://ops.xxx.com（本期本地用 http://localhost:3000）
用户登录的是 Personal Ops，不是每次直接登录 GitHub。
```

### 3.2 登录方式
- **主登录**：GitHub 登录（页面只有一个主按钮"使用 GitHub 登录"）。
- 登录成功后后台检查：是否指定 GitHub 账号 / 是否允许访问指定仓库 / 是否白名单。GitHub 只负责确认身份。
- **后续快捷登录**：个人设备保持登录（手机/个人电脑 30 天，公司电脑 8 小时）。
- 未来再加：指纹 / Face ID / Touch ID / Passkey / 扫码登录（MVP 不做）。

### 3.3 登录页设计
```
┌─────────────────────────┐
│  Personal Ops            │
│  你的个人知识与任务控制台  │
│  [ 使用 GitHub 登录 ]     │
│  □ 这是我的个人设备        │
└─────────────────────────┘
```
"这是我的个人设备"勾选 → 登录保持更久、缓存最近内容、支持快捷解锁；不勾 → 几小时后退出、不缓存敏感内容、关闭浏览器清除登录。用于区分个人电脑和公司电脑。

### 3.4 登录状态表（必须实现的状态）
| 状态 | 页面表现 |
|---|---|
| 未登录 | 显示 GitHub 登录按钮 |
| 登录中 | 加载状态 |
| 登录成功 | 进入首页（本期 = 知识库页） |
| 非白名单账号 | 提示无访问权限 |
| 登录过期 | 要求重新登录 |
| 网络断开 | 展示最近缓存内容 |
| GitHub 授权失效 | 提示重新授权 |
| 账号被移除 | 强制退出 |
| 临时设备 | 关闭浏览器后退出 |

### 3.5 正确的权限关系（安全约束）
```
用户终端 → Personal Ops 登录系统 → 后台服务器 → GitHub App → GitHub 私有仓库
```
- 用户登录成功后，浏览器**只获得 Personal Ops 登录会话**，不直接拿 GitHub Token。
- GitHub 密钥只保存在服务器。
- 每台设备可单独退出、可远程踢出丢失设备、可统一控制仓库权限。

> **v2 现状**：`useAuthStore` 的 `login(isPersonal)` 是**模拟实现**——直接返回一个写死的 JK 用户对象并设 `sessionExpiresAt`，未接 GitHub OAuth。接手时需替换为真实 OAuth 流程（见第 10 节）。

---

## 4. 产品经理需输出的文档清单（原始规划，作为验收基线）

第一版建议准备五份内容：

1. **页面原型**：登录页、首页、知识库页、任务页、AI 审批页、账号与设备页。
2. **登录流程图**：打开网站 → 检查登录状态 → 未登录跳 GitHub → GitHub 返回身份 → 检查白名单 → 创建会话 → 进入首页。
3. **权限表**：
   | 操作 | 人 | AI |
   |---|---|---|
   | 查看知识库 | 可以 | 可以 |
   | 创建修改 | 可以 | 可以生成草稿 |
   | 直接提交 GitHub | 可以 | **不可以** |
   | 审批 AI 修改 | 可以 | **不可以** |
   | 删除正式文件 | 可以 | **不可以** |
   | 发布公开站点 | 可以 | 只能准备内容 |
4. **多端适配规则**：手机底部导航、电脑侧边导航、功能和数据一致、公司电脑默认短登录、个人设备长期登录。
5. **验收标准**：
   - 三个终端能通过同一网址登录。
   - 登录后能看到同一批 GitHub 数据。
   - 某终端提交修改后，其他终端能看到最新版本。
   - AI 修改必须经过人工批准。
   - 公司电脑退出后不能继续访问。

---

## 5. 第一版范围建议（v1 规划，v2 已据此精简）

**第一版做**：GitHub 登录 + 知识库浏览 + 任务看板 + AI 修改审批 + 三端响应式界面。

**第一版不做**：独立手机 App、复杂扫码登录、多人组织权限、离线编辑、实时多人协同编辑。

**第一版核心体验**：在任何设备打开同一网址，登录后看到同一份 GitHub 知识库，并能审批 AI 准备好的修改。

---

## 6. 当前实现状态（v2，2026-07-19）

### 6.1 v2 重构决策（与 v1 的差异）
用户在 v1（7 页面）基础上要求：
- **删除**：首页、任务看板、审批中心三个页面（遵循"第一版不做"原则，且简化聚焦）。
- **知识库改为默认首页**，并映射用户真实 Obsidian 目录结构（`00_Inbox / 01_Raw / 02_Operations / 03_Wiki / 99_System`）。
- **AI 执行改为「AI 助理」**：定位从"任务执行"改为"AI 工具评估与使用数据看板"（参考用户上传的《AI 工具评估与使用数据看板 V1》需求）。
- **设计语言**：参考 Apple design 人机协同与 HMI 规范，极简化（纯黑背景、毛玻璃、0.5px 分割线、SF Pro 风格字体）。

### 6.2 已实现页面（4 个）
| 路由 | 页面 | 功能 |
|---|---|---|
| `/login` | 登录页 | 模拟 GitHub 登录，"个人设备"复选框控制会话时长 |
| `/`（默认） | 知识库 | 左侧文件树（映射真实目录）+ 搜索 + 右侧内容预览 |
| `/ai-assistant` | AI 助理 | 统计卡片 + 折线图（每日 Token 趋势）+ 环形图（工具占比）+ 工具使用明细表 + 工具筛选 |
| `/settings` | 设置 | 账号信息、设备信息、PWA 安装/离线状态、退出登录 |

### 6.3 已实现能力
- ✅ 响应式布局：桌面 `≥768px` 左侧边栏导航；手机底部导航。
- ✅ PWA：manifest + service worker（`autoUpdate`），可"添加到主屏幕"、离线缓存静态资源。
- ✅ 登录态持久化：Zustand `persist` 中间件写入 `localStorage`（key: `personal-ops-auth`）。
- ✅ 版本检测清理：`main.tsx` 检测 `personal-ops-version`，版本变化自动清旧数据（防结构不兼容白屏）。
- ✅ ErrorBoundary：渲染错误时显示错误信息而非静默白屏。

### 6.4 已知是 Mock / 未实现（重要，接手时勿误以为已完成）
- ⚠️ **登录**：`login()` 直接返回写死用户，未接 GitHub OAuth / 白名单 / 过期真实判断（仅 `sessionExpiresAt` 时间判断）。
- ⚠️ **知识库数据**：`src/store/data.ts` 中 `knowledgeTree` / `fileContents` / `searchIndex` 为**手写 mock**，未从 GitHub 仓库读取。点击文件展示的是 `fileContents` 里预设的文本。
- ⚠️ **AI 助理数据**：`dailyTokenData` 由 `generateDailyData()` 生成模拟数据；`toolEvaluations` 为 6 个工具的评估 mock；图表、统计均为前端计算 mock。
- ⚠️ **任务看板 / AI 审批**：`src/types/index.ts` 已定义 `Task` / `AIJob` 类型，但**页面未实现**（v2 精简删除）。
- ⚠️ **编辑/提交 GitHub**：未实现。

---

## 7. 技术架构

### 7.1 技术栈
| 项 | 选型 |
|---|---|
| 构建 | Vite 5 |
| 框架 | React 18 + TypeScript 5 |
| 路由 | React Router 6（`BrowserRouter`，无嵌套 Router） |
| 状态 | Zustand 4（`persist` 中间件仅用于 auth） |
| PWA | vite-plugin-pwa 0.21（`registerType: autoUpdate`，Workbox 生成 SW） |
| 图表 | 手写 SVG（无图表库，见 `AIAssistantPage.tsx` 的 line/donut 绘制） |
| 测试验证 | puppeteer-core + 系统 Chrome（headless）做端到端截图验证 |

### 7.2 目录结构
```
personal-ops/
├── index.html
├── package.json
├── vite.config.ts          # PWA 配置、GitHub API 缓存策略
├── tsconfig.json / tsconfig.node.json
├── assets/
│   ├── favicon.svg
│   └── icons/              # icon-192/512 (.png + .svg)
├── src/
│   ├── main.tsx            # 入口 + 版本检测清理 localStorage
│   ├── App.tsx             # 路由：未登录→/login；已登录→Layout+3路由
│   ├── types/index.ts      # 全部类型（AuthUser/KBFile/Task/AIJob...）
│   ├── store/
│   │   ├── index.ts        # useAuthStore / useKnowledgeStore / useAIAssistantStore
│   │   └── data.ts         # 所有 mock 数据（knowledgeTree/fileContents/dailyTokenData/...）
│   ├── hooks/useDevice.ts  # 设备类型检测（mobile/tablet/desktop）+ 在线状态
│   ├── utils/helpers.ts    # formatNumber/formatCost/calcChange/getToolColor 等
│   ├── components/
│   │   ├── Layout.tsx      # 侧边栏(桌面)/底部导航(手机) + main-content
│   │   └── ErrorBoundary.tsx
│   ├── pages/
│   │   ├── LoginPage.tsx
│   │   ├── KnowledgePage.tsx
│   │   ├── AIAssistantPage.tsx
│   │   └── SettingsPage.tsx
│   └── styles/global.css   # 全部样式（CSS 变量 + 暗色主题 + 响应式）
```

### 7.3 路由与守卫
- `App.tsx` 读取 `useAuthStore().user`，`user && sessionExpiresAt > Date.now()` 为真才渲染 `Layout + Routes`，否则渲染 `/login`。
- ⚠️ **注意**：当前守卫仅用 `sessionExpiresAt` 判断，无"非白名单/账号被移除/GitHub 授权失效"等状态（见第 3.4 节状态表，尚未全部实现）。

### 7.4 状态管理（Zustand）
- `useAuthStore`：`user / isPersonalDevice / login() / logout()`，persist 到 `localStorage['personal-ops-auth']`。
- `useKnowledgeStore`：`currentPath / currentContent / searchQuery / expandedDirs / selectFile() / toggleDir() / setSearchQuery() / searchResults`，**未 persist**（每次启动重置）。
- `useAIAssistantStore`：`period / chartMetric / selectedTools / getFilteredData() / getSummary() / getToolBreakdown() / getDailyTrend() / getRanking()`，**未 persist**。

### 7.5 设计系统要点（Apple HMI 极简）
定义在 `src/styles/global.css` 的 CSS 变量：
- 背景：纯黑 `#000`（body）→ 卡片用 `--color-surface` 系列半透明深色。
- 主色：`--color-primary`（蓝 `#0A84FF` 风格），用于激活态/主按钮。
- 分割线：`0.5px solid var(--color-separator)`（Apple 风格细线）。
- 毛玻璃：`backdrop-filter: blur(20px)`（侧边栏、底部导航、卡片）。
- 字体：系统字体栈（`-apple-system, SF Pro` 优先），等宽用 `--font-mono`。
- 圆角：`--radius-md` 等。
- 安全区：`--safe-top / --safe-bottom`（刘海屏适配）。

---

## 8. 页面规格细节（供 UI 还原/扩展）

### 8.1 知识库页（默认首页 `/`）
- 布局：左文件树（`.kb-sidebar`）+ 右内容区（`.kb-content`），flex 横向。
- 文件树：`knowledgeTree`（5 个一级目录）。文件夹可展开/折叠（`expandedDirs` Set 控制）；文件夹显示 `fileCount` 徽章；长名 `text-overflow: ellipsis` + `title` hover 全名。
- 搜索：顶部输入框，输入后 `searchResults` 覆盖树视图，显示标题/路径/预览片段。
- 内容区：选中文件后展示 `currentContent`（`<pre>` 保留换行）；未选显示空状态（书本图标 + 引导文案）。
- 映射：树结构对应 `~/obsidian_vault` 的 `00_Inbox / 01_Raw / 02_Operations / 03_Wiki / 99_System`。

### 8.2 AI 助理页（`/ai-assistant`）
- 顶部：标题「AI 助理」+ 时间筛选（近7天/近30天/全部，控制 `period`）。
- 统计卡片（6 个，`.metrics-grid` 桌面 6 列等宽）：总 Token、总费用、日均 Token、日均费用、使用天数、工具数量。带"vs 上期"变化率（当前 mock 偏固定，待真实数据）。
- 工具筛选 chips：6 个工具（ChatGPT/Claude/Kimi/混元/GLM/DeepSeek），彩色点，可多选切换 `selectedTools`。
- 每日 Token 趋势：手绘 SVG 折线图，可切「总计/输入/输出」（`chartMetric`）。
- 工具使用占比：SVG 环形图 + 图例百分比。
- 工具使用明细：表格（Token/费用/任务数 + 横向进度条 `bar-fill`）。
- 数据来源：全部来自 `useAIAssistantStore` 的计算函数（基于 `dailyTokenData` + `toolEvaluations`）。

### 8.3 设置页（`/settings`）
- 账号卡：用户、登录方式(GitHub 徽章)、设备类型(个人/临时 徽章)、会话剩余(小时)。
- 设备卡：设备类型(💻/📱)、网络状态(在线/离线 绿点)。
- PWA 卡：添加到主屏幕（按钮触发 `alert` 提示浏览器菜单操作）、离线缓存(已启用 徽章)。
- 退出登录：红色按钮 → `logout()` + 跳 `/login`。

### 8.4 登录页（`/login`）
- 居中卡片：⚡ logo + 标题 + 副标题 + 「使用 GitHub 登录」主按钮 + 「这是我的个人设备」复选框（默认勾选）+ 会话时长提示。
- 点击登录：`setLoading` → 1s 后 `login(isPersonal)` → `navigate('/')`。

---

## 9. 响应式与多端规则
- **断点**：`max-width: 768px` 为手机。
- **桌面（≥768px）**：`.sidebar` 左侧固定 240px 导航；`.bottom-nav` 隐藏；`.main-content` padding `24px 32px`。
- **手机（<768px）**：`.sidebar` 隐藏；`.bottom-nav` 固定底部显示（知识库/AI 助理/设置）；`.main-content` padding `16px` + 底部留白避让 nav。
- **知识库/AI 助理**在手机端布局需各自适配（当前未专门做手机端内部重排，仅整体响应式）。

---

## 10. 下一步开发路线（给接手 AI 的优先级清单）

> 按优先级排序。每一项都标注了当前代码里的"接入口"。

### P0 — 真实 GitHub 登录（替换 mock）
- **接入口**：`src/store/index.ts` 的 `useAuthStore.login()`、`src/pages/LoginPage.tsx`、`vite.config.ts` 已配 GitHub API 缓存。
- **任务**：
  1. 接入 GitHub OAuth（OAuth App 或 GitHub App），用真实 `code` 换 `access_token`。
  2. 后台校验白名单账号 + 仓库访问权限（本期无后台，可先前端校验用户名/组织，但生产必须服务端）。
  3. 实现第 3.4 节全部登录状态（非白名单/过期/授权失效/账号移除）。
  4. 密钥只存服务端，前端只持 Personal Ops 会话（见 3.5 安全约束）。

### P0 — 真实知识库数据
- **接入口**：`src/store/data.ts`（`knowledgeTree`/`fileContents`/`searchIndex` 替换）、`useKnowledgeStore`、`KnowledgePage.tsx`。
- **任务**：用 GitHub API（`api.github.com/repos/{owner}/{repo}/git/trees`、`contents`）拉取文件树与文件内容；搜索改为对仓库内容检索（或本地索引）。离线时读 service worker 缓存。

### P1 — 任务看板页
- **接入口**：`src/types/index.ts` 已有 `Task` 类型；新增 `src/pages/TasksPage.tsx` + `App.tsx` 路由 + `Layout` 导航项。
- **任务**：看板视图（todo/in_progress/done/blocked），支持创建/编辑/删除，提交 GitHub（经审批）。

### P1 — AI 审批中心
- **接入口**：`src/types/index.ts` 已有 `AIJob` 类型；新增 `src/pages/ApprovalPage.tsx`。
- **任务**：展示 AI 生成的 diff，用户 批准/修改后批准/驳回（权限表约束：AI 不能直接提交）。

### P2 — 首页汇总视图
- **接入口**：`App.tsx` 路由 + 新 `HomePage.tsx`。
- **任务**：登录后先进入产品化首页（待办数/AI 执行中/待审批/最近更新），而非直接文件树。

### P2 — 数据真实化与打磨
- AI 助理页接入真实 AI 工具使用数据（替换 `generateDailyData()`）。
- mock 数据的 100% 增长、环形图 99.9% 合计等打磨项。
- 移动端各页面内部布局重排。

### 远期（原"第一版不做"）
独立手机 App、扫码登录、多人组织权限、离线编辑、实时协同。

---

## 11. 开发环境指令

```bash
# 安装依赖（开发源目录）
cd ~/.qclaw/workspace/personal-ops-pwa
npm install

# 启动开发服务器（HMR，默认 http://localhost:5173，vite.config 配了 host）
npm run dev

# 生产构建
npm run build          # 输出 docs/（GitHub Pages）

# 预览构建产物
npm run preview        # http://localhost:3000

# PWA 要求：service worker 仅在 production build + https/localhost 生效
```

**验证方法**（本项目用 puppeteer-core + 系统 Chrome headless 做端到端截图）：
```js
// 例：node pw-check.cjs（需 puppeteer-core，executablePath 指向系统 Chrome）
// 覆盖：登录→知识库→AI助理→设置 全流程 + 截图标错 + console error 捕获
```

---

## 12. 关键文件索引（接手必读）

| 文件 | 作用 | 接手关注点 |
|---|---|---|
| `src/App.tsx` | 路由 + 登录守卫 | 改路由/加页面从这里 |
| `src/store/index.ts` | 三个 Zustand store | 真实登录/数据接入的主战场 |
| `src/store/data.ts` | 全部 mock 数据 | 替换为真实数据来源的入口 |
| `src/types/index.ts` | 全类型定义 | Task/AIJob 已定义待实现 |
| `src/components/Layout.tsx` | 导航 + 主内容区 | 加导航项、调响应式 |
| `src/pages/*.tsx` | 四个页面 | UI 还原与扩展 |
| `src/styles/global.css` | 设计系统 | CSS 变量、暗色主题、响应式断点 |
| `vite.config.ts` | PWA + GitHub 缓存 | 改 manifest、SW 策略 |
| `src/main.tsx` | 入口 + 版本清理 | localStorage 兼容性逻辑 |

---

## 13. 历史开发日志
- `dev-logs/personal-ops-pwa_v2_20260719.md` — v2 重构启动记录
- `dev-logs/personal-ops-pwa_white-screen-fix_20260719.md` — 白屏修复（dailyData 变量名）
- `dev-logs/personal-ops-pwa_white-screen-fix-v2_20260719.md` — 白屏修复（localStorage 旧数据）
- `dev-logs/personal-ops-pwa_ui-fixes_20260719.md` — UI 布局修复（设置页 padding、stat 卡片 6 列、树名 ellipsis）
- `dev-logs/personal-ops-pwa_dev_20260719.md` — 早期开发记录
